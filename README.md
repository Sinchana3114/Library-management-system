# 📚 Library Management System (Python + Tkinter + SQLite)

A simple desktop application for managing books, users, and borrowing history.  
Built with **Python**, **Tkinter GUI**, and **SQLite database**.

## 🚀 Features
- Add, Update, Delete, and Search books (CRUD operations)
- Role-based login access (can be extended)
- Local SQLite database storage
- User-friendly interface using Tkinter

## 🛠️ Technologies Used
- Python 3
- Tkinter
- SQLite3

## ▶️ How to Run
1. Clone the repository  
   ```bash
   git clone https://github.com/<your-username>/Library-Management-System.git
   cd Library-Management-System
